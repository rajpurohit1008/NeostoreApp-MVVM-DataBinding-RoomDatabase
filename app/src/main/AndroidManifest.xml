<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rajpurohit.neostoreapp">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NeoStoreApp"
        android:usesCleartextTraffic="true">
        <activity android:name=".viewactivity.AddresslistActivity"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyAXk0UurocU9ERQINKTw0M4g2GjrC-Yxgs" />

        <activity
            android:name=".viewactivity.StoreLocatorActivity"
            android:label="@string/title_activity_store_locator" />
        <activity android:name=".viewactivity.StorelocationActivity" />
        <activity android:name=".viewactivity.ResetpasswordActivity" />
        <activity android:name=".viewactivity.EditprofileActivity" />
        <activity android:name=".viewactivity.MyaccountActivity" />
        <activity android:name=".viewactivity.OrderidActivity" />
        <activity android:name=".viewactivity.MyorderActivity" />
        <activity android:name=".viewactivity.AddressActivity" />
        <activity android:name=".viewactivity.MycartActivity" />
        <activity android:name=".viewactivity.ForgotpasswordActivity" />
        <activity android:name=".viewactivity.ProductdetailActivity" />
        <activity android:name=".viewactivity.TableActivity" />
        <activity
            android:name=".viewactivity.HomeActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".viewactivity.RegisterActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".viewactivity.LoginActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".viewactivity.MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>